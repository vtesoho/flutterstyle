import 'package:flutter/material.dart';

class TestClipRect extends StatelessWidget {
  const TestClipRect({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text(
          'text'
        ),
      ),
    );
  }
}